# Force Railway to use the root Dockerfile instead of Nixpacks
# The Dockerfile properly installs Playwright/Chromium for PDF generation

[build]
builder = "dockerfile"
dockerfilePath = "../Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
