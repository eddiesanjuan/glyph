# Glyph Dashboard Configuration
# Local development
VITE_API_URL=http://localhost:3000

# Production (Railway) - NOTE: This is a build-time variable, not runtime!
# For Railway, the default is baked into the Dockerfile.
# Override via Railway Variables > VITE_API_URL if needed.
# VITE_API_URL=https://glyph-api-production-3f73.up.railway.app

# Glyph PDF Generation
GLYPH_API_KEY=gk_your_api_key_here
